import React from "react";

export const metadata = {
  title: "Tickets",
  description: "Welcome to the Tickets page of our repair shop application.",}

const Tickets = () => {
  return <div>Tickets</div>;
};

export default Tickets;
